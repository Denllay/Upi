import { Box } from '@mui/material';
import React from 'react';

interface Param {
  nick: string;
  repository: string;
}

const Repository = () => {
  return <Box></Box>;
};

export default Repository;
